The component playground for the point-of-vue globe.

This package uses vite and histoire in `dev` mode while both are built to be statically served in both the `dist` and `playground` folders respectively.
